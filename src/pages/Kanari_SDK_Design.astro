---
import ArticlePage from "../components/ArticlePage.astro";
import { processMarkdown } from "../utils/markdownProcessor";

const post = await Astro.glob("../content/Kanari_SDK_Design.md");
const contentHtml = await processMarkdown(post[0].rawContent());
---

<ArticlePage
  title="Inside the Kanari SDK"
  description="A map of the Rust workspace, Move runtime, DAG engine, RPC layers, and developer entry points."
  contentHtml={contentHtml}
  relatedHref="/Kanari_SDK_Implementation"
  relatedTitle="Building with KanariClient"
  relatedDescription="Use the Flutter and Dart facade for queries, signed transactions, and Move function calls."
  relatedIndex="02"
/>
