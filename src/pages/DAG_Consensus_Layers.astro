---
import ArticlePage from "../components/ArticlePage.astro";
import { processMarkdown } from "../utils/markdownProcessor";

const post = await Astro.glob("../content/DAG_Consensus_Layers.md");
const contentHtml = await processMarkdown(post[0].rawContent());
---

<ArticlePage
  title="DAG Consensus Layers"
  description="Understand how application logic, DAG execution, committee decisions, data availability, persistence, and sync fit together."
  contentHtml={contentHtml}
  previous={{
    href: "/Architecture",
    title: "Kanari DAG Architecture",
    description: "Follow a signed transaction through vertices, quorum decisions, checkpoints, canonical replay, and persistence.",
    index: "03",
  }}
  kicker="Consensus guide"
  meta="Layer reference"
/>
